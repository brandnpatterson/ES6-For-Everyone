// Template Strings
const name = 'Snickers'
const age = 2
const sentance = `My dog ${name} is ${age * 7} years old`
console.log(sentance)


const markup = `
  <div class="person">
    <h2>${person.name}</h2>
    <p class="location">${peros}
  </div>
`
